<h1>Editing Gallery</h1>

<% form_for(:gallery, :url => {:action => 'update', :id=> @gallery}, :html => { :multipart => true}) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag 'Update' %> or <%= link_to 'Cancel', :action => :index %>
<% end %>